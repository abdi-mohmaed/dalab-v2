generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:C:/Users/user/dalab/prisma/test.db"
}

model Test {
  id String @id @default(cuid())
}
